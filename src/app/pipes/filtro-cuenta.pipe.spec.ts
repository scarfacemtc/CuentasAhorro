import { FiltroCuentaPipe } from './filtro-cuenta.pipe';

describe('FiltroCuentaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCuentaPipe();
    expect(pipe).toBeTruthy();
  });
});
